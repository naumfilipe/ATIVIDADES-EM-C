#include <stdio.h> //chamada da biblioteca padrão C

main() //início da função principal
{
int g=0,i=0,l=0,k=0; //declaração e inicialização das variáveis
int m, n, o; //declaração de variáveis do tipo inteiro

while (i<10) //declaração da estrutura de repetição (enquanto)
{	
	m=++g; //incremetação da variavel "g"
	n=+l; //substituição da variavel "n" pela varaivel "l"
	o=++k; //incrementação da variael "k"
	i++; //contador da variavel "i"
}
printf (" m=%d,n=%d,o=%d ", m,n,o); //impressão da das variáveis
}
